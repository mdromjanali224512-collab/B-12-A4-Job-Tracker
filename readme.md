Answers to Questions
1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
ans:-Difference is
gelElementById=select a uniq id,
getElementsByClassName= select a number of same class name,
querySelector / querySelectorAll= these selector select css class name single/many.


2. How do you create and insert a new element into the DOM?
ans:-
oneElement,
const div=document.createElement('div')
oneElement.append(div)


3. What is Event Bubbling? And how does it work?
ans:-eventBabbling is a way that even propagate though dom such as when usher click an element select this element after that parents,grandparent and so on.


4. What is Event Delegation in JavaScript? Why is it useful?
ans:- Event Delegation is a technique of javaScript when you want a specific chaild select then you use this technique.


5. What is the difference between preventDefault() and stopPropagation() methods?
ans:-preventDefault() it is use in from and stopPropagation() use in javaScript code.